{
  "name": "binary-split",
  "version": "1.0.5",
  "description": "a fast newline (or any delimiter) splitter stream - like require('split') but faster",
  "main": "index.js",
  "scripts": {
    "test": "standard --verbose && node test.js"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/maxogden/binary-split.git"
  },
  "author": "max ogden",
  "license": "BSD-2-Clause",
  "bugs": {
    "url": "https://github.com/maxogden/binary-split/issues"
  },
  "dependencies": {
    "through2": "^2.0.3"
  },
  "devDependencies": {
    "standard": "^12.0.1",
    "tape": "~4.9.1"
  },
  "files": [
    "index.js"
  ]
}
